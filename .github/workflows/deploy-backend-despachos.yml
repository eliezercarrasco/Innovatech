name: CI/CD Backend Despachos - Deploy to EC2

on:
  push:
    branches:
      - main
      - deploy
    paths:
      - "back-Despachos_SpringBoot/**"
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-backend-despachos:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/innovatech-backend-despachos:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-to-ec2:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        with:
          host: ${{ secrets.EC2_BACKEND_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: DOCKERHUB_USERNAME,DOCKERHUB_TOKEN
          script: |
            set -e

            REPO_DIR=/home/ubuntu/innovatech
            REPO_URL=https://github.com/eliezercarrasco/Innovatech.git

            # Clonar repositorio si no existe, o actualizar si ya existe
            if [ -d "$REPO_DIR/.git" ]; then
              cd $REPO_DIR && git pull origin main
            else
              git clone $REPO_URL $REPO_DIR
              cd $REPO_DIR
            fi

            # Autenticar Docker Hub para evitar rate limits en pulls
            echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

            # Obtener la nueva imagen del backend despachos
            docker compose -f $REPO_DIR/docker-compose.prod.yml pull backend-despachos

            # Reiniciar solo el backend despachos sin afectar BD ni otros servicios
            docker compose -f $REPO_DIR/docker-compose.prod.yml up -d --no-deps backend-despachos

            # Limpiar imágenes antiguas para liberar espacio en disco
            docker image prune -f

            echo "Deploy backend-despachos completado exitosamente"
